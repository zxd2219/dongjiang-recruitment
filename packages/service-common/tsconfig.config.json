{
  "extends": "./tsconfig.json",
  "include": [
    "vite.config.*",
    "scripts/**/*.*",
    "vitest.config.*",
    "rollup.config.*",
    "cypress.config.*",
    "tailwind.config.*",
    "playwright.config.*"
  ],
  "compilerOptions": {
    "composite": true,
    "types": ["node"]
  }
}
